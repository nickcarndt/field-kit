# The API key lives in server/.env locally and in Railway's dashboard in
# prod — it must never be committed.
server/.env
server/.venv/
build/
*.egg-info/
__pycache__/
.pytest_cache/
*.pyc
.DS_Store
.claude/
